============
Installation
============

At the command line::

    $ easy_install django_eighty_days

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_eighty_days
    $ pip install django_eighty_days
